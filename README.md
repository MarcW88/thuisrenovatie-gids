# Thuisrenovatie Gids

Static foundation for the decision-first renovation platform. Run `python3 generate_pages.py` to regenerate all empty editorial routes, then serve the repository root with any static server.
