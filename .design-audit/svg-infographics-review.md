# SVG infographic remediation

Final review branch for the 20 controlled Dutch SVG infographic assets. This file intentionally triggers PR checks after the generated-site commit so the rendered audit runs against the final branch head.
